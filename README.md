# loopback-sdk-react
Loopback SDK React

# Installation

Using npm:

```javascript
$ npm i -g npm
$ npm i --save loopback-sdk-react
```

Using yarn:

```javascript
$ npm i -g yarn
$ yarn add loopback-sdk-react
```
